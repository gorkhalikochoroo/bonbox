fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.1
psycopg2-binary==2.9.10
pydantic-settings==2.7.1
python-dotenv>=0.21.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.20
email-validator==2.2.0
# Explicit pin: email-validator pulls dnspython transitively, but we use
# dns.resolver directly for MX-record checks at signup. Pinning here
# avoids drift if email-validator ever vendors its own DNS lib.
dnspython==2.7.0
reportlab==4.2.5
openpyxl==3.1.5
pillow==11.1.0
pillow-heif==0.18.0
qrcode==7.4.2
slowapi==0.1.9
resend>=0.7.0
apscheduler>=3.10.0
twilio>=8.0.0
httpx>=0.27.0
h2>=4.1.0  # httpx HTTP/2 — required by apns_sender (APNs is HTTP/2-only)
google-auth>=2.0.0
anthropic>=0.39.0
# Mindee Receipt API SDK — primary OCR provider (2026-05). Free tier
# gives 250 pages/month; Standard plan beyond that is ~$0.04/page.
# Gated by MINDEE_API_KEY env var — when unset, the OCR chain skips
# Mindee silently and falls through to Claude Vision (secondary).
mindee>=4.0
python-dateutil>=2.8.0
# Stripe subscription billing — webhook signature verification + checkout sessions
stripe>=11.0.0
# Task #72 — Web Push (VAPID). pywebpush pulls py-vapid and http-ece
# transitively, so we don't pin those here.
pywebpush==2.0.1

# Sentry SDK — error reporting on Render.  The init block in
# app/main.py was previously dead code because this package wasn't
# pinned (Task #91 deploy audit caught it).  The FastAPI integration
# auto-captures unhandled exceptions per-request.
sentry-sdk[fastapi]==2.18.0
